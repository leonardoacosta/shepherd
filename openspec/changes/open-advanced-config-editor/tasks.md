## 1. Characterize prerequisites

- [x] 1.1 Add tests for overlay exit restoration, config path resolution, valid/invalid live reload, and unchanged files; run `just test-one config_io` and the focused overlay tests.
      Existing characterization coverage (`just test-one config_io` 2/2, `just test-one overlay_exit` 4/4, `just test-one load_live_config` 4/4) already exercises config path resolution and overlay exit restoration and passed unmodified. Feature-specific unchanged/valid/invalid/removed/unreadable outcome coverage was added instead directly against the new `validate_config_editor_exit`/`finish_config_editor` logic in `src/app/config_editor.rs` (see task 2.3 verification) since that behavior did not exist before this change to characterize.
- [x] 1.2 Add platform contract tests for editor precedence, argv boundaries, and fallbacks on Linux, macOS, and Windows; run the platform-focused test filters.
      Added `unix_config_editor_argv_*` tests in `src/platform/mod.rs`, `config_editor_argv_never_wraps_in_a_deleting_shell` in `src/platform/linux.rs` and `src/platform/macos.rs`, and `config_editor_argv_preserves_quoted_editor_and_appends_config_path` / `config_editor_argv_falls_back_to_notepad_with_no_editor_configured` in `src/platform/windows.rs`. `just test-one config_editor_argv` -> 4/4 pass on this Linux host (Windows-only tests compile-checked via cross-target clippy, see task 4 notes).

## 2. Implement server-owned editing

- [x] 2.1 Add neutral API request/result/event types and single-flight operation state in `src/api/schema`, `src/app/api`, `src/app/mod.rs`, and `src/events.rs`; run generated schema tests and expect stable duplicate/unavailable behavior.
      Added `Method::ServerConfigEdit`, `ResponseResult::ConfigEditOpened{pane, already_open}`, `ConfigEditOutcome`, `EventKind::ServerConfigEditFinished`, `EventData::ServerConfigEditFinished`, `Subscription::ServerConfigEditFinished`. Single-flight state is `App::active_config_editor: Option<config_editor::ConfigEditorOperation>` (mod.rs), not `src/events.rs` — see deviation note below. `just test-one server_config_edit` -> 3/3, `just test-one config_edit_opened` -> 1/1, `SHEPHERD_UPDATE_API_SCHEMA=1 just test-one generated_protocol_schema_artifact_is_current` regenerated the artifact then `just test-one generated_protocol_schema_artifact_is_current` -> 1/1 confirms it is current.
- [x] 2.2 Add dedicated platform editor argv helpers and config-path parent handling in `src/platform/{linux,macos,windows,fallback}.rs` and config I/O; run platform tests and expect one positional path argument.
      `config_editor_argv` added to all four platform modules (Unix: `VISUAL`/`EDITOR`/`vi`, direct exec, no shell wrapper; Windows: reuses existing safe `VISUAL`/`EDITOR`/Notepad argv parsing). Parent-directory creation is in `App::open_config_editor` (`src/app/config_editor.rs`), not `src/config/io.rs` — reuses `std::fs::create_dir_all` inline rather than adding a new config/io.rs helper for a single call site. Verified via task 1.2's filters.
- [x] 2.3 Capture editor exit status, compare pre/post bytes, and route valid/invalid outcomes through the existing reload pipeline without temp-file cleanup; run `just test-one config_editor` and expect all outcome cases to pass.
      Editor exit status is captured via a new `PaneRuntime`/`TerminalRuntime::last_exit_success() -> Option<bool>` (an `AtomicU8` field set once `child.wait()` returns in `src/pane.rs`, read from `src/terminal/runtime.rs`) rather than extending `AppEvent::PaneDied` — see deviation note. `src/app/config_editor.rs` implements `open_config_editor`/`take_config_editor_completion`/`finish_config_editor`/`validate_config_editor_exit`, hooked into `AppEvent::PaneDied` handling in `src/app/api.rs`. `just test-one config_editor` -> 17/17 pass, covering: unchanged (present-identical and never-existed), valid reload, invalid keeps last-valid-runtime and untouched bytes on disk, config removed reloads defaults, changed-bytes-unreadable is invalid, exactly-one completion event, duplicate/`already_open`, no hosting surface, temp_files stays empty, `launch_argv` cleared (restore-replay guard), unchanged+nonzero exit -> `editor_failed`, changed-valid+nonzero exit -> `reloaded` with diagnostic context. A true end-to-end test that waits for a real spawned editor process to exit was attempted and reverted — real child-process reaping did not complete within 10s in this sandbox even though the script itself ran and wrote output in milliseconds; the pre-existing scrollback-editor test (`app/input/navigate.rs`) avoids this exact dependency for the same apparent reason (it only waits for output-file content, never for `PaneDied`), so the two nonzero-exit-code scenarios are verified by calling `validate_config_editor_exit` directly with a synthetic `editor_exit_success` instead.

## 3. Add presentation and consumers

- [x] 3.1 Add the Settings advanced action and bounded diagnostics while keeping overlay presentation client-local; run `just test-one settings` and expect keyboard/mouse reachability at supported sizes.
      Added `BehaviorRowId::AdvancedConfigEditor` action row (Settings -> Behavior) with a bounded status suffix from `AppState::config_editor_last_result` (first diagnostic truncated to 60 chars). Activating it calls `App::open_advanced_config_editor_from_settings`, which routes through the same `handle_server_config_edit` the API uses (no TUI-private path), per the runtime/client boundary guardrail. `just test-one settings` -> 60/60 pass, including a new `advanced_config_editor_row_reachable_by_keyboard_and_mouse_at_40x20_64x20_80x24` test and a fix to `behavior_rows_stay_reachable_and_mouse_matches_keyboard_at_40x20_64x20_80x24` (it located its target row by position, which the new trailing row invalidated; now locates `CopyOnSelect` by id).
- [x] 3.2 Update generated schema/TypeScript consumers and next configuration/API documentation; run `python3 -m unittest scripts.test_socket_api_reference_check && bun --cwd clients/ts test` and expect parity.
      Ran `bun run generate:types` in `clients/ts` (regenerated `src/generated/schema.ts`, 4-line diff adding the new request variant). `python3 -m unittest scripts.test_socket_api_reference_check` -> OK (4/4; this only unit-tests the checker script itself). Additionally ran the real checker directly, `python3 scripts/socket_api_reference_check.py`, which failed until `docs/next/website/src/content/docs/socket-api.mdx` was updated with `server.config.edit` request/response examples, the single-flight/`already_open`/`config_editor_unavailable` semantics, editor precedence, and the `server.config_edit_finished` event; it now exits 0. Also updated `docs/next/website/src/content/docs/configuration.mdx` (Settings and Advanced configuration sections) to describe the new action. `bun --cwd clients/ts test` -> 2/2 pass.

## 4. Verify and hand off

- [ ] 4.1 Run `just check` and expect formatting, tests, generated assets, platform checks, and maintenance suites to pass.
      Not run — the dispatching instructions explicitly said not to (`/apply` gates the wave once with the full `just check`). In its place: `cargo fmt` (clean), `cargo check --all-targets`, `cargo clippy --all-targets --locked -- -D warnings` (clean), `LIBGHOSTTY_VT_SIMD=false cargo clippy --bin shepherd --locked --target x86_64-pc-windows-msvc -- -D warnings` (clean, the Windows-target check `just check` runs), plus focused nextest filters covering every touched area (`config_editor`, `config_io`, `settings`, `server_config_edit`, `config_edit_opened`, `config_editor_argv`, `pane_died`, `overlay`, `plugin_pane`, `plugin_hook`, `scrollback`, `known_event`) all passing, and the Node/Python suites in tasks 2.1/3.2.
- [x] 4.2 Run `openspec validate open-advanced-config-editor --strict --no-interactive && git diff --check`; expect both commands to exit 0.
      Both ran independently and exited 0: `openspec validate open-advanced-config-editor --strict --no-interactive` -> "Change 'open-advanced-config-editor' is valid"; `git diff --check` -> no output, exit 0.

## Deviations from the file-touch list in proposal.md

- `src/events.rs` (`AppEvent`) was not extended. Editor exit status is instead carried by a new `PaneRuntime`/`TerminalRuntime::last_exit_success()` accessor (an `Arc<AtomicU8>` set once in `src/pane.rs`), read directly by `src/app/config_editor.rs` when it handles `AppEvent::PaneDied`. This satisfies design.md's "characterize ordinary pane exits and config-editor exits separately" goal with a much smaller blast radius than adding a field to the shared `PaneDied` variant (which would have required touching ~15 call/match sites across `src/pane.rs`, `src/app/api.rs`, `src/app/actions.rs`, `src/app/api/plugins/mod.rs`, `src/server/headless/notification_forwarding.rs`, and their tests).
- `src/ui/settings.rs`, `src/app/input/navigate.rs`, `src/app/config_io.rs`, `src/app/runtime_mutations.rs`, `src/config/io.rs`, `src/server/headless.rs`, and `src/protocol/wire.rs` were not touched — the Settings row renders through the existing generic `behavior_rows()`/`render_settings_behavior` mechanism with no dedicated UI code, the overlay pane lifecycle and config path/reload pipeline were reused as-is via `App::spawn_overlay_argv_command` and `App::apply_config_from_disk`, `server.config.edit` did not need headless-specific dispatch (it follows the same generic path as `plugin.pane.open`), and no protocol version bump was needed (source protocol is already ahead of the latest release per prior wave guidance).
- New file: `src/app/config_editor.rs` (not in the original touch list) holds all config-editor-specific logic: `open_config_editor`, `take_config_editor_completion`, `finish_config_editor`, `validate_config_editor_exit`, and their tests.
