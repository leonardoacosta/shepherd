## 1. Characterize current authority

- [x] 1.1 Add terminal tests for screen evidence, exclusive lifecycle evidence, mixed evidence, screen-overridden mixed evidence, clear/expiry, and persisted-session-only cases; run `just test-one agent_state_source` and expect all arbitration cases to pass.
  - note: strict red->green was not possible — `agent_state_evidence()` and its supporting types did not exist, so a test referencing them is a Rust compile error, not a red test. Added the neutral `AgentStateEvidence`/`EffectiveStateEvidence`/`StateEvidenceAuthority`/`StateEvidenceReporter` types and the `TerminalState::agent_state_evidence()` helper in `src/terminal/state.rs` together with the 6 characterization tests, then ran green. `just test-one agent_state_source`: 6 passed.
- [x] 1.2 Add a parity characterization proving PaneInfo and AgentInfo use the same terminal projection; run `just test-one agents` and expect identical evidence values.
  - note: same red->green caveat as 1.1 — `AgentInfo`/`PaneInfo.state_source` did not exist yet, so this was staged together with 2.1/2.2. Added `agent_info_and_pane_info_share_state_source_projection` and `agent_info_and_pane_info_agree_for_exclusive_lifecycle_authority` to `src/app/agents.rs`. `just test-one agents`: 34 passed.

## 2. Implement the typed projection

- [x] 2.1 Add shared state-evidence schema types and terminal projection logic in `src/terminal/state.rs`, `src/api/schema/agents.rs`, and `src/api/schema/panes.rs`; run focused Rust tests and expect screen/reported authority values to serialize deterministically.
  - note: schema types (`AgentStateSource`, `AgentStateEffective`, `AgentStateAuthority`, `AgentStateReporter`) landed in `src/api/schema/common.rs` (the existing shared-type home for `AgentStatus`/`PaneAgentState`, reused by both `agents.rs` and `panes.rs` via `use super::common::{...}`) rather than duplicated into both files; both schema files import and use them. `cargo build --tests`: clean, no warnings.
- [x] 2.2 Wire `src/app/creation.rs` and `src/app/agents.rs` to the shared helper and derive `screen_detection_skipped` from effective exclusive evidence; run `just test-one agent_state_source` and expect parity and compatibility tests to pass.
  - note: `just test-one agent_state_source`: 6 passed. `just test-one agents`: 34 passed (includes the 1.2 parity tests).

## 3. Add client observations

- [x] 3.1 Add exact-source reporter/session aggregation to the integration view model without exposing identifiers or health colors; run `just test-one integration` and expect canonical, absent, and unknown-source cases to pass.
  - note: `integration_target_for_source` (exact `shepherd:<label>` match) in `src/integration/registry.rs`; `IntegrationObservation` + `integration_observations()` (pure, takes a terminal iterator) in `src/integration/types.rs`; wired into the Settings row status text via `integration_status_text` in `src/ui/settings.rs` (counts/wording stay client-side, no session identifiers or health colors). Added `Hash` to `IntegrationTarget` (`src/api/schema/integrations.rs`) since it is now a `HashMap` key. `just test-one integration`: 145 passed. `just test-one settings`: 59 passed.
- [x] 3.2 Update generated schema, TypeScript client tests, and next API documentation; run `python3 -m unittest scripts.test_socket_api_reference_check && bun --cwd clients/ts test` and expect generated contracts to match.
  - note: regenerated `docs/next/api/shepherd-api.schema.json` via `SHEPHERD_UPDATE_API_SCHEMA=1 cargo test --bin shepherd generated_protocol_schema_artifact_is_current` (196-line diff adding the 4 new schema types + `state_source` on `PaneInfo`/`AgentInfo`); re-ran without the env var to confirm it's now stable. Documented `state_source` in `docs/next/website/src/content/docs/socket-api.mdx`. `clients/ts/src/generated/schema.ts` does not expand nested response payload types (`ResponseResult`/`PaneInfo`/`AgentInfo` stay as bare type references, a pre-existing generator limitation unrelated to this change) — regenerated it and confirmed zero diff, `generate:types:check` passes. `python3 -m unittest scripts.test_socket_api_reference_check`: 4 passed. `bun --cwd clients/ts test`: 2 passed.

## 4. Verify and hand off

- [x] 4.1 Compare `src/protocol/wire.rs::PROTOCOL_VERSION` with the latest release, update fixtures only if required, then run `just check` and expect all repository checks to pass.
  - note: re-verified — `git tag --sort=-version:refname` still shows `v0.7.5` as latest, `src/protocol/wire.rs::PROTOCOL_VERSION` is still `19` (unchanged by this wave), ahead of the released `17`. Per CLAUDE.md's release-relative rule, did not bump it. All of this wave's changes are additive (new optional `state_source` fields with `skip_serializing_if`, new enum types) — no existing wire fixture, and no `PaneInfo`/`AgentInfo` literal in `src/protocol/wire.rs`, needed changes. Per the dispatch instructions I did NOT run `just check` — that is the team-lead's wave gate. `cargo build --tests` is clean across the whole crate (confirmed after fixing all 4 call sites that construct `PaneInfo` literals).
- [x] 4.2 Run `openspec validate expose-agent-state-source --strict --no-interactive && git diff --check`; expect both commands to exit 0.
  - note: `openspec validate`: "Change 'expose-agent-state-source' is valid", exit 0. `git diff --check`: exit 0 (no whitespace errors).
