# Tasks — build-api-pane-output-stream

1. Add the experimental schema and schema tests.
2. Implement bounded per-subscriber output queues with explicit gap markers.
3. Wire the producer path into `events.subscribe`.
4. Add an example/smoke consumer.
5. Verify focused API/subscription tests, then archive.
